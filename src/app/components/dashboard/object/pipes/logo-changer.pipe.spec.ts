import { LogoChangerPipe } from './logo-changer.pipe';

describe('LogoChangerPipe', () => {
  it('create an instance', () => {
    const pipe = new LogoChangerPipe();
    expect(pipe).toBeTruthy();
  });
});
